# Screenshots

The report includes interface screenshots generated from the project UI. The screenshots use the same HTML/CSS/JS interface and demo seed data when MySQL is not available in the build environment.
